import { Home, Heart } from "lucide-react";
import styles from "./Logo.module.css";

const Logo = () => {
  return (
    <div className={styles.logo}>
      <Home size={28} />     
     
    </div>
  );
};

export default Logo;
